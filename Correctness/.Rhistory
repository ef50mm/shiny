incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
correctCount = sum(correct$DeviceCount)
incorrectCount = sum(incorrect$DeviceCount)
incorrectCount / (correctCount + incorrectCount)
}
results3 <- matrix(incorrectnessRate(combos$i, combos$j), nrow=length(appVersions))
View(results3)
combos <- expand.grid(appVersion=appVersions, date=dates)
View(combos)
results3 <- matrix(incorrectnessRate(combos$appVersion, combos$date), nrow=length(appVersions))
View(results3)
results3 <- matrix(incorrectnessRate(combos$appVersion, combos$date), nrow=length(appVersions))
incorrectnessRate(combos$appVersion, combos$date)
combos$appVersion
combos$date
incorrectnessRate(combos$appVersion, combos$date)
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
correctCount = sum(correct$DeviceCount)
incorrectCount = sum(incorrect$DeviceCount)
print(incorrectCount / (correctCount + incorrectCount))
}
incorrectnessRate(combos$appVersion, combos$date)
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
correctCount = sum(correct$DeviceCount)
incorrectCount = sum(incorrect$DeviceCount)
result <- (incorrectCount / (correctCount + incorrectCount))
print(result)
result
}
View(combos)
x = ["17.3.4597.0109", "17.3.4604.0120"]
y = [as.Date("2014-11-01", "%Y-%m-%d"), as.Date("2014-11-02", "%Y-%m-%d")]
combos <- expand.grid(x=x, y=y)
x = ["17.3.4597.0109", "17.3.4604.0120"]
x = ("17.3.4597.0109", "17.3.4604.0120")
x = c("17.3.4597.0109", "17.3.4604.0120")
y = c(as.Date("2014-11-01", "%Y-%m-%d"), as.Date("2014-11-02", "%Y-%m-%d"))
combos <- expand.grid(x=x, y=y)
View(combos)
incorrectnessRate(combos$x, combos$y)
View(combos)
incorrectnessRate("17.3.4597.0109", as.Date("2014-11-02", "%Y-%m-%d"))
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
correctCount = sum(correct$DeviceCount)
incorrectCount = sum(incorrect$DeviceCount)
result <- (incorrectCount / (correctCount + incorrectCount))
result
}
incorrectnessRate("17.3.4597.0109", as.Date("2014-11-02", "%Y-%m-%d"))
incorrectnessRate("17.3.4597.0109", as.Date("2014-11-01", "%Y-%m-%d"))
incorrectnessRate(combos$x, combos$y)
incorrectnessRate("17.3.4597.0120", as.Date("2014-11-01", "%Y-%m-%d"))
incorrectnessRate("17.3.4597.0120", as.Date("2014-11-02", "%Y-%m-%d"))
x = c("17.3.4597.0109", "17.3.4604.0120")
y = as.Date("2014-11-01", "%Y-%m-%d")
combos <- expand.grid(x=x, y=y)
incorrectnessRate(combos$x, combos$y)
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
print(correct)
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
print(incorrect)
correctCount = sum(correct$DeviceCount)
incorrectCount = sum(incorrect$DeviceCount)
result <- (incorrectCount / (correctCount + incorrectCount))
result
}
incorrectnessRate(combos$x, combos$y)
incorrectnessRate <- function(build, date) {
print(build, date)
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
print(correct)
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
print(incorrect)
correctCount = sum(correct$DeviceCount)
incorrectCount = sum(incorrect$DeviceCount)
result <- (incorrectCount / (correctCount + incorrectCount))
result
}
incorrectnessRate(combos$x, combos$y)
incorrectnessRate <- function(build, date) {
print(build)
print(date)
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
print(correct)
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
print(incorrect)
correctCount = sum(correct$DeviceCount)
incorrectCount = sum(incorrect$DeviceCount)
result <- (incorrectCount / (correctCount + incorrectCount))
result
}
combos <- expand.grid(x=x, y=y)
incorrectnessRate(combos$x, combos$y)
combos <- expand.grid(x=x, y=y)
combos
incorrectnessRate("17.3.4597.0120", as.Date("2014-11-02", "%Y-%m-%d"))
reliability$Date == y
z <- reliability[reliability$Date == y, ]
View(z)
y = c(as.Date("2014-11-01", "%Y-%m-%d"), as.Date("2014-11-02", "%Y-%m-%d"))
z <- reliability[reliability$Date == y, ]
reliability$Date == y
View(z)
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
print(incorrect)
correctCount = sum(correct$DeviceCount)
incorrectCount = sum(incorrect$DeviceCount)
incorrectCount / (correctCount + incorrectCount)
}
results <- matrix(, nrow = length(appVersions), ncol = length(dates))
for (r in 1:length(appVersions)) {
for (c in 1:length(dates)) {
results[r, c] <- incorrectnessRate(r, c)
}
}
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
correctCount = sum(correct$DeviceCount)
incorrectCount = sum(incorrect$DeviceCount)
incorrectCount / (correctCount + incorrectCount)
}
results <- matrix(, nrow = length(appVersions), ncol = length(dates))
for (r in 1:length(appVersions)) {
for (c in 1:length(dates)) {
results[r, c] <- incorrectnessRate(r, c)
}
}
results2 <- matrix(, nrow = length(appVersions), ncol = length(dates))
for (r in 1:length(appVersions)) {
results2[r, ] <- incorrectnessRate(r, dates)
}
all.equal(results, results2)
results3 <- matrix(, nrow = length(appVersions), ncol = length(dates))
results3 <- matrix(, nrow = length(appVersions), ncol = length(dates))
results3 <- incorrectnessRate(outer(rep(1, length(appVersions)), c(1:length(appVersions))), dates)
rep(appVersions, length(appVersions))
outer(rep(1, length(appVersions)), c(1:length(appVersions)))
combos <- expand.grid(appVersion=appVersions, date=dates)
results3 <- matrix(incorrectnessRate(combos$appVersion, combos$date), nrow=length(appVersions))
combos <- expand.grid(appVersion=appVersions, date=dates)
y <- incorrectnessRate(combos$appVersion, combos$date)
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
correctCount = sum(correct$DeviceCount)
incorrectCount = sum(incorrect$DeviceCount)
incorrectCount / (correctCount + incorrectCount)
}
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
correctCount = sum(correct$DeviceCount)
incorrectCount = sum(incorrect$DeviceCount)
incorrectCount / (correctCount + incorrectCount)
}
results <- matrix(, nrow = length(appVersions), ncol = length(dates))
for (r in 1:length(appVersions)) {
for (c in 1:length(dates)) {
results[r, c] <- incorrectnessRate(appVersions[r], dates[c])
}
}
View(results)
View(results)
startDate = as.Date("2014-11-25", "%Y-%m-%d")
reliability$Date[reliability$Date > startDate]
dates <- sort(unique(reliability$Date[reliability$Date > startDate]))
dates
dates
rows <- length(appVersions)
columns <- length(dates)
results <- matrix(, nrow = length(appVersions), ncol = length(dates))
for (r in 1:length(appVersions)) {
for (c in 1:length(dates)) {
results[r, c] <- incorrectnessRate(appVersions[r], dates[c])
}
}
View(results)
appVersions
knownBuilds <- readRDS("knownBuilds.rds")
View(knownBuilds)
reliability <- readRDS("reliability.rds")
reliability$Date <- as.Date(reliability$Date, "%Y-%m-%d")
dates <- sort(unique(reliability$Date))
dates
populationThreshold = 100
View(reliability)
library(dplyr)
test <- incorrectDevices %>%
filter(Name %in% (0, 1, 2, 3, 5)) %>%
group_by(AppVersion) %>%
ungroup() %>%
test <- incorrectDevices %>%
filter(Name %in% (0, 1, 2, 3, 5)) %>%
group_by(AppVersion) %>%
ungroup()
test <- incorrectDevices %>%
filter(Name %in% (0, 1, 2, 3, 5)) %>%
group_by(AppVersion) %>%
ungroup()
test <- reliability %>%
filter(Name %in% (0, 1, 2, 3, 5)) %>%
group_by(AppVersion) %>%
ungroup()
test <- aggregate(DeviceCount ~ AppVersion, reliability, sum)
View(test)
test <- aggregate(DeviceCount ~ AppVersion, reliability[reliability$Name %in% c(0, 1, 2, 3, 5), ], sum)
View(test)
knownBuilds <- readRDS("knownBuilds.rds")
knownBuilds$ReleaseDate <- as.Date(knownBuilds$ReleaseDate, "%Y-%m-%d")
knownBuilds <- knownBuilds[knownBuilds$Monitor == 1, ]
releases <- knownBuilds[knownBuilds$ReleaseDate > cutoffDate, ]
View(results)
cutoffDate = as.Date("2014-10-25", "%Y-%m-%d")
populationThreshold = 100
knownBuilds <- readRDS("knownBuilds.rds")
knownBuilds$ReleaseDate <- as.Date(knownBuilds$ReleaseDate, "%Y-%m-%d")
knownBuilds <- knownBuilds[knownBuilds$Monitor == 1, ]
releases <- knownBuilds[knownBuilds$ReleaseDate > cutoffDate, ]
reliability <- readRDS("reliability.rds")
reliability$Date <- as.Date(reliability$Date, "%Y-%m-%d")
appVersions <- sort(unique(knownBuilds$AppVersion))
dates <- sort(unique(reliability$Date))
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
correctCount = sum(correct$DeviceCount)
incorrectCount = sum(incorrect$DeviceCount)
incorrectCount / (correctCount + incorrectCount)
}
results2 <- matrix(, nrow = length(appVersions), ncol = length(dates))
for (r in 1:length(appVersions)) {
results2[r, ] <- incorrectnessRate(appVersions[r], dates)
}
View(results2)
appVersions
View(results2)
z <- incorrectnessRate("17.3.5816.0312", dates)
View(results2)
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
correctCount = sum(correct$DeviceCount)
incorrectCount = sum(incorrect$DeviceCount)
return(incorrectCount / (correctCount + incorrectCount))
}
results2 <- matrix(, nrow = length(appVersions), ncol = length(dates))
for (r in 1:length(appVersions)) {
results2[r, ] <- incorrectnessRate(appVersions[r], dates)
}
View(results2)
combos <- expand.grid(appVersion=appVersions, date=dates)
x = c("17.3.4597.0109", "17.3.4604.0120")
y = c(as.Date("2014-11-01", "%Y-%m-%d"), as.Date("2014-11-02", "%Y-%m-%d"))
combos <- expand.grid(x=x, y=y)
incorrectnessRate(combos$x, combos$y)
incorrectnessRate(combos$x, combos$y)
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
print(dim(correct))
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
print(dim(incorrect))
correctCount = sum(correct$DeviceCount)
incorrectCount = sum(incorrect$DeviceCount)
return(incorrectCount / (correctCount + incorrectCount))
}
incorrectnessRate(combos$x, combos$y)
x = c("17.3.1229.0918", "17.3.4501.1020")
y = c(as.Date("2014-11-01", "%Y-%m-%d"), as.Date("2014-11-02", "%Y-%m-%d"))
combos <- expand.grid(x=x, y=y)
incorrectnessRate(combos$x, combos$y)
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
print(dim(correct))
print(dim(incorrect))
correctCount = sum(correct$DeviceCount)
print(dim(correctCount))
incorrectCount = sum(incorrect$DeviceCount)
print(dim(incorrectCount))
return(incorrectCount / (correctCount + incorrectCount))
}
incorrectnessRate(combos$x, combos$y)
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
print(dim(correct))
print(dim(incorrect))
correctCount = sum(correct$DeviceCount)
print(length(correctCount))
incorrectCount = sum(incorrect$DeviceCount)
print(length(incorrectCount))
return(incorrectCount / (correctCount + incorrectCount))
}
incorrectnessRate(combos$x, combos$y)
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
print(dim(correct))
print(dim(incorrect))
correctCount <- aggregate(correct, list(buid, date), sum)
print(length(correctCount))
incorrectCount <- aggregate(incorrect, list(buid, date), sum)
print(length(incorrectCount))
return(incorrectCount / (correctCount + incorrectCount))
}
incorrectnessRate(combos$x, combos$y)
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
print(dim(correct))
print(dim(incorrect))
correctCount <- aggregate(correct, list(build, date), sum)
print(length(correctCount))
incorrectCount <- aggregate(incorrect, list(build, date), sum)
print(length(incorrectCount))
return(incorrectCount / (correctCount + incorrectCount))
}
incorrectnessRate(combos$x, combos$y)
list(combos$x, combos$y)
build <- combos$x
date <- combos$y
build
date
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
View(incorrect)
correctCount <- aggregate(correct, list(build, date), sum)
correctCount <- aggregate(correct$DeviceCount, list(build, date), sum)
correctCount <- aggregate(correct$DeviceCount, list(build=correct$AppVersion, date=correct$Date), sum)
View(correctCount)
incorrectCount <- aggregate(incorrect$DeviceCount, list(build=incorrect$AppVersion, date=incorrect$Date), sum)
incorrectCount / (correctCount + incorrectCount)
correctCount + incorrectCount
View(correctCount)
incorrectCount$x / (correctCount$x + incorrectCount$x)
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
correctCount <- aggregate(correct$DeviceCount, list(build=correct$AppVersion, date=correct$Date), sum)
incorrectCount <- aggregate(incorrect$DeviceCount, list(build=incorrect$AppVersion, date=incorrect$Date), sum)
return(incorrectCount$x / (correctCount$x + incorrectCount$x))
}
results2 <- matrix(, nrow = length(appVersions), ncol = length(dates))
for (r in 1:length(appVersions)) {
results2[r, ] <- incorrectnessRate(appVersions[r], dates)
}
View(results2)
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
correctCount <- aggregate(correct$DeviceCount, list(build=correct$AppVersion, date=correct$Date), sum)
incorrectCount <- aggregate(incorrect$DeviceCount, list(build=incorrect$AppVersion, date=incorrect$Date), sum)
return(incorrectCount$x / (correctCount$x + incorrectCount$x))
}
results2 <- matrix(, nrow = length(appVersions), ncol = length(dates))
for (r in 1:length(appVersions)) {
results2[r, ] <- incorrectnessRate(appVersions[r], dates)
}
View(results2)
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
print(dim(correct))
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
correctCount <- aggregate(correct$DeviceCount, list(build=correct$AppVersion, date=correct$Date), sum)
print(length(correct))
incorrectCount <- aggregate(incorrect$DeviceCount, list(build=incorrect$AppVersion, date=incorrect$Date), sum)
print(length(correctCount))
rate <- (incorrectCount$x / (correctCount$x + incorrectCount$x))
print(length(rate))
}
incorrectnessRate(appVersions[1], dates)
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
print(dim(correct))
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
correctCount <- aggregate(correct$DeviceCount, list(build=correct$AppVersion, date=correct$Date), sum)
print(length(correct))
incorrectCount <- aggregate(incorrect$DeviceCount, list(build=incorrect$AppVersion, date=incorrect$Date), sum)
print(length(correctCount))
rate <- (incorrectCount$x / (correctCount$x + incorrectCount$x))
print(length(rate))
return(rate)
}
incorrectnessRate(appVersions[1], dates)
length(dates)
dim(reliability)
incorrectnessRate(combos$x, combos$y)
combos <- expand.grid(appVersion=appVersions, date=dates)
results3 <- matrix(incorrectnessRate(combos$appVersion, combos$date), nrow=length(appVersions))
View(results3)
combos <- expand.grid(appVersion=appVersions, date=dates)
incorrectnessRate(combos$appVersion, combos$date)
combos$appVersion
combos$date
incorrectnessRate
incorrectnessRate(combos$appVersion, combos$date)
combos <- expand.grid(appVersion=appVersions, date=dates)
build <- combos$appVersion
date <- combos$date
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
results <- matrix(, nrow = length(appVersions), ncol = length(dates))
for (r in 1:length(appVersions)) {
for (c in 1:length(dates)) {
results[r, c] <- incorrectnessRate(appVersions[r], dates[c])
}
}
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
print(dim(correct))
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
if (nrow(correct) == 0 | nrow(incorrect) == 0) {
return NA
}
correctCount <- aggregate(correct$DeviceCount, list(build=correct$AppVersion, date=correct$Date), sum)
print(length(correct))
incorrectCount <- aggregate(incorrect$DeviceCount, list(build=incorrect$AppVersion, date=incorrect$Date), sum)
print(length(correctCount))
rate <- (incorrectCount$x / (correctCount$x + incorrectCount$x))
print(length(rate))
return(rate)
}
incorrectnessRate <- function(build, date) {
correct <- reliability[(reliability$Name %in% c(0, 1, 5)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
print(dim(correct))
incorrect <- reliability[(reliability$Name %in% c(2, 3)) & (reliability$AppVersion == build) & (reliability$Date == date), ]
if (nrow(correct) == 0 | nrow(incorrect) == 0) {
return(NA)
}
correctCount <- aggregate(correct$DeviceCount, list(build=correct$AppVersion, date=correct$Date), sum)
print(length(correct))
incorrectCount <- aggregate(incorrect$DeviceCount, list(build=incorrect$AppVersion, date=incorrect$Date), sum)
print(length(correctCount))
rate <- (incorrectCount$x / (correctCount$x + incorrectCount$x))
print(length(rate))
return(rate)
}
results <- matrix(, nrow = length(appVersions), ncol = length(dates))
for (r in 1:length(appVersions)) {
for (c in 1:length(dates)) {
results[r, c] <- incorrectnessRate(appVersions[r], dates[c])
}
}
View(results)
saveRDS(results, "results.rds")
View(results)
names(results)
results
matplot(results[:,], results[,:])
results[:,]
(-4:5)^2
matplot((-4:5)^2, main = "Quadratic")
matplot(results)
results
df <- data.frame()
df.(appVersions[1]) <- dates
df$(appVersions[1]) <- dates
df = data.frame(matrix(vector(), 0, 3, dimnames=list(c(), appVersions)))
df = data.frame(matrix(vector(), 0, length(appVersions), dimnames=list(c(), appVersions)))
df = data.frame(matrix(vector(), length(appVersions), 0, dimnames=list(appVersions, c())))
rownames(results)
nrows(results)
nrow(results)
rownames(results) <- appVersions
rownames(results)
View(results)
colnames(results)
colnames(results) <- appVersions
colnames(results) <- dates
colnames(results)
colnames(results) <- format(dates, format="%Y-%m-%d")
colnames(results)
View(results)
df <- data.frame(results)
View(df)
View(df)
results <- readRDS("results.rds")
results <- transpose(results)
results <- t(results)
View(results)
rownames(results) <- format(dates, format="%Y-%m-%d")
colnames(results) <- appVersions
df <- data.frame(results)
View(df)
View(df)
ggplot(aes(x = as.Date(row.names, "%Y-%m-%d"), y = X17.3.1229.0918), data = df) +
geom_point()
ggplot(aes(x = row.names, y = X17.3.1229.0918), data = df) +
geom_point()
df$row.names <- as.Date(df$row.names, "%Y-%m-%d")
df$row.names
names(df)
df$Date <- dates
ggplot(data = df, aes(Date, X17.3.1229.0918)) + geom_line()
matplot(results, type = c("b"),pch=1,col = 1:10)
matplot(results, type = c("l"), pch=1, col = 1:10)
matplot(results, type = c("b"), pch=1, col = 1:10)
x = c("17.3.1229.0918", "17.3.4501.1020")
y = c(as.Date("2014-11-01", "%Y-%m-%d"), as.Date("2014-11-02", "%Y-%m-%d"), as.Date("2014-11-03", "%Y-%m-%d"))
combos <- expand.grid(x=x, y=y)
combos <- expand.grid(x=appVersion, y=date)
